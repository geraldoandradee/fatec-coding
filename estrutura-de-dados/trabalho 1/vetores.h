#include <stdio.h>

int gera_vetor_crescente(int *, int);  // gera um vetor crescente com limite definido no segundo parametro
int gera_vetor_decrescente(int *, int);  // gera um vetor decrescente com limite definido no segundo parametro

#define LIMIT 100000